import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: `<router-outlet></router-outlet>`, // Use template inline para teste  // Mude para usar um arquivo HTML separado
})
export class AppComponent {}
